**Building on the previous lab...**
1. Move the models into a file called models.py and import that file using "import models"  
*Hint: move any dependent import statements and variables along with the models*